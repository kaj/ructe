[package]
name = "statics"
version = "0.4.0"
authors = ["Rasmus Kaj <kaj@kth.se>"]

build = "src/build.rs"

[build-dependencies]
ructe = { path = "../..", features = ["sri", "mime03"] }

[dependencies]
mime = "0.3.0"
